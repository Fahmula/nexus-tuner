# pyproject.toml

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nexus-stream"
version = "0.0.1"  # placeholder for local installation

[tool.setuptools]
package-dir = {"" = "nexus_stream"}